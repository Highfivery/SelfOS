# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
dist-web/
out/
build/
.vite/
*.tsbuildinfo

# Capacitor / iOS build artifacts (the ios/ project itself is committed; these are generated)
apps/desktop/ios/App/App/public/
apps/desktop/ios/App/Pods/
apps/desktop/ios/DerivedData/

# Test / coverage
coverage/
playwright-report/
test-results/

# Secrets & local env (never commit the Claude API key)
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.log
*.local

# Claude Code — personal/local overrides (project settings.json IS committed)
.claude/settings.local.json

# Electron packaging artifacts
release/
