// @selfos/core/host — platform host interfaces injected into the business logic (07-mobile-platform §5.1).
// SecretStore + ClaudeClient join FileSystem in later slices.
export * from './fileSystem';
