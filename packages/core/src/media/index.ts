// @selfos/core/media — generic encrypted-media storage (45-session-attachments §5.1), parameterized by a
// vault dir + a caller-supplied path guard. Built reusable; surfaced only in Sessions for now (CLAUDE.md §12).
export * from './mediaService';
